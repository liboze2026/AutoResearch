// Package experimentrun hosts the stage2 experiment run skeleton.
//
// It will later contain run lifecycle helpers, state transition guards, and
// run-scoped artifact metadata, while reusing the existing MRAG service and
// repository layers.
package experimentrun
