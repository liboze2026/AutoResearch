// Package runner hosts the stage2 remote runner contract skeleton.
//
// It will serve as the execution-facing layer for experiment runs while
// reusing the existing SSH gateway and remote command execution infrastructure.
package runner
