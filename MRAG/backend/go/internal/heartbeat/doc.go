// Package heartbeat hosts the stage2 server heartbeat skeleton.
//
// It is intended to extend the current server status refresh capability into a
// queryable heartbeat chain without replacing the servers table as the primary
// server registry.
package heartbeat
