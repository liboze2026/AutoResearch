# Stage2 Python Templates

This directory hosts reusable experiment execution templates.

Current templates:

- `mock_train_template`: minimal rule-based training template for stage2

Each template should provide:

- `train.py`
- `evaluate.py`
- `config_schema.json`
- `runner.py`
- `README.md`
