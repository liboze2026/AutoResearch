# Stage2 Python Runners

This directory is reserved for stage2 experiment runner entrypoints.

Planned usage:

- minimal remote/local runner adapters
- runner-side status reporters
- lightweight scripts that map an ExperimentSpec into executable commands

This directory is intentionally scaffold-only for now.
